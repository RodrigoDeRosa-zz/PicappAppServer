language: python
python:
  - 3.6

install:
  - pip install -r requirements.txt
  - pip install coverage

script:
  - python -m unittest tests/test_stories.py
  - coverage erase
  - coverage run --source=. -m unittest discover -s tests/
  - coverage report -m